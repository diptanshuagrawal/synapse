pyyaml>=6.0
requests>=2.31
anthropic>=0.40
numpy>=1.26   # embedding/cluster math (ask_engine, cluster_subjects, validate_embeddings)
python-dateutil>=2.8   # RRULE expansion for recurring calendar events (derive/meetings/calendar_feed.py)

# Dev / test only (not needed by ingest or cron):
#   .venv/bin/python -m pip install pytest
# or run bin/run-tests.sh which installs it on first use.
pytest>=8.0
pytest-cov>=5.0   # coverage floor (--cov-fail-under) on the full-suite gate
httpx>=0.27   # test_anthropic_client imports it directly; don't rely on anthropic's transitive pull (it drifted and broke CI)
